version: '3.8'

services:
  postgres:
    container_name: financel-control-postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=finance-control-database