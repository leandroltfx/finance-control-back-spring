version: '3.8'

services:
  postgres:
    container_name: finance_control_database
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=finance_control_database_postgres